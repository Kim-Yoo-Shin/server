devtools:
  livereload:
    enabled: true

spring:
  output.ansi.enabled: always
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sa?serverTimezone=UTC&characterEncoding=UTF-8  # for local test
    username: root
    password: 1234

  jpa:
    show-sql: true
    hibernate.ddl-auto: create


logging.level.com.example: info